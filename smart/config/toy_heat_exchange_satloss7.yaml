defaults:
  - toy_heat_exchange
  - toy_heat_exchange_satloss7_protocol
  - _self_

experiment:
  name: "TOY_HEAT_EXCHANGE_SMART_SATLOSS"
  model_name: "SMART_TOY_HEAT_EXCHANGE_SATLOSS7"
  model_tag: ""
  # SMART keeps the same base architecture but trains from scratch for 300
  # epochs. The other model families use the shared 150-epoch continuation.
  epochs: 300
